# Panda Home Station - Development Environment Example
# Copy this file to nas/.env (same directory as nas/scripts/) or export in your shell.
# Do NOT commit your actual credentials.

# Server Configuration
# Required: PostgreSQL connection string used by run_dev.sh (nasserver dev)
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=

# Optional: Backend API port for nasserver dev (default: 8000)
PNAS_API_PORT=8001
PNAS_STATIC_PORT=8081

# Optional: Dev storage root path used by nasserver dev
# If unset, defaults to <repo>/nas/fs
PNAS_STORAGE_PATH=

# Frontend Configuration
# Optional: Frontend dev server port for Web Desktop (default: 5173)
FRONTEND_PORT=5173